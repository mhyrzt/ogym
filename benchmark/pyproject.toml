[project]
name = "benchmark"
version = "0.1.0"
description = "Performance comparison between ogym and Gymnasium environments"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "gymnasium[box2d,mujoco]>=1.2.2",
    "matplotlib>=3.5.0",
    "scienceplots>=2.1.1",
]
