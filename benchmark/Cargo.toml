[package]
name = "benchmark"
version = "0.1.0"
edition = "2024"

[dependencies]
ogym = { path = "../", features = ["mujoco"] }
nalgebra = "0.33"

[patch.crates-io]
mujoco-rust = { path = "../vendor/mujoco-rust" }
