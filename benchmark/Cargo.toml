[package]
name = "benchmark"
version = "0.1.0"
edition = "2024"

[features]
default = []
mujoco = ["ogym/mujoco"]

[dependencies]
ogym = { path = "../", default-features = false }
nalgebra = "0.33"

[patch.crates-io]
mujoco-rust = { path = "../vendor/mujoco-rust" }
